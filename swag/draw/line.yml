parameters:
  - name: start_x
    description: Horizontal pixel position to start with.
    in: formData
    type: integer
    minimum: 0
    default: 0
    required: true
  - name: start_y
    description: Vertical pixel position to start with.
    in: formData
    type: integer
    minimum: 0
    default: 0
    required: true
  - name: stop_x
    description: Horizontal pixel position to end with.
    in: formData
    type: integer
    minimum: 0
    default: 0
    required: true
  - name: stop_y
    description: Vertical pixel position to end with.
    in: formData
    type: integer
    minimum: 0
    default: 0
    required: true
  - $ref: '#/definitions/form_param_r'
  - $ref: '#/definitions/form_param_g'
  - $ref: '#/definitions/form_param_b'
  - $ref: '#/definitions/form_param_push_immediately'

responses:
  '200':
    description: OK

tags:
  - draw
